# 问题匹配器测试说明

## 已实现的功能

1. **问题匹配器配置** (package.json)
   - `$c51` - 用于C51编译器
   - `$c251` - 用于C251编译器
   - `$armcc` - 用于ARMCC编译器
   - `$gcc` - 用于GCC/ARMCLANG编译器

2. **编译任务实现** (PTarget.ts)
   - 使用VSCode Task API执行编译任务
   - 自动设置problemMatchers
   - 编译输出重定向到日志文件
   - 错误/警告自动显示在问题窗口

## 测试方法

1. 打开一个Keil项目
2. 在代码中制造一个编译错误（例如：缺少分号）
3. 点击"编译"按钮（F7）
4. 查看问题窗口是否显示错误信息
5. 点击错误信息是否能跳转到对应文件和行号

## 预期结果

- 编译错误/警告会自动显示在VSCode的问题窗口
- 点击错误信息可以跳转到对应的文件和行号
- 支持C51、C251、ARMCC、GCC等多种编译器

## 实现原理

1. **Task API**: 使用VSCode的Task API执行编译命令
2. **Problem Matchers**: 通过正则表达式解析编译输出
3. **自动集成**: 错误/警告自动显示在问题窗口，无需手动解析
